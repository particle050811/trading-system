# 关键 Prompt 记录

精选开发中 5 条代表性 prompt，按"场景 → AI 初版 → 我发现的问题 → 我要求的修正"四段呈现。重点是**我如何审查 AI 产出并把方向掰回来**。

---

## 1. 挂单簿用什么数据结构

**场景**：撮合引擎要按"价格优先、时间优先"匹配买卖盘——价格更优的单先成交，价格相同时挂得早的先成交。

**AI 初版**：AI 选了"有序数组 + 二分插入"——挂单时二分找位置，插进去；撤单时找到位置再删。理由是"挂单量百到千级别，移动元素的代价不大"。

**我发现的问题**：挂单/撤单频繁时，数组每次插入或删除都要把后面所有元素整体移动，吃满数组撮合时累计代价会被 n 放大；而且自己写有序结构容易踩坑。

**我的 Prompt 节选**：
> 撮合的核心数据结构应该是跳表，不要用数组撑。同价位用先进先出队列保留时间优先，跨价档用跳表排序。**不要自己写跳表**——直接装 npm 上的 `proper-skip-list`。

**修正后**：
- 挂单簿改成两层结构：外层跳表按价格排序，内层每个价位是一条同价队列
- 撮合时跳表能从最优档往外按顺序扫，相邻档跳过去几乎不花时间
- 同价队列里头部就是挂得最早的那笔，天然满足时间优先
- 跳表实现细节多（随机层级、双向遍历、空键回收），自己写大概率有 bug，所以装库

实现踩坑：跳表遍历时不能边走边删当前节点（会破坏指针），所以先把要删的价位记下，遍历完再统一删。

代码：`back-end/src/store/orders.ts`、`back-end/src/engine/matcher.ts`、跳表行为单测 `back-end/src/store/orders.test.ts`。

---

## 2. 金额怎么存才不丢精度

**场景**：金融系统对小数误差零容忍——账面差一分钱长期对账就会暴。

**AI 初版**：AI 用浮点小数存金额（单位：元），还在持仓上挂了"持仓均价"字段每次更新；下单校验也用浮点比较。

**我发现的问题**：JS 浮点 `0.1 + 0.2 ≠ 0.3` 这种事在金融账面上是灾难；均价是个推导出来的显示值，存在数据库里会和实际成本对不上。

**我的 Prompt 节选**：
> 金额永远不准用浮点。全部改成"分"（整数），持仓只存"股数 / 总成本（分） / 冻结股数"三个整数，**均价不要存**——这是显示问题，让前端自己算。再帮我推一遍：卖出时按比例扣成本，清仓那一笔会不会留下"被四舍五入吞掉的零碎"？

**修正后**：
- 全部金额改成整数分；下单设了上限（单价不超过 100 万分、数量不超过 100 万股），保证就算多笔成交累加也不会撑爆 JS 整数范围
- 持仓只留三个整数字段，挡住未来"再加个均价字段"的冲动
- AI 推完证明：清仓那一笔是把全部股数都卖掉，公式上扣的成本恰好等于剩余总成本，**精确归零、跨周期不残留**
- 不变量证明写进 `reservation.ts` 注释；单测锁住"清仓后再买入均价无残留"

代码：`back-end/src/store/users.ts`、`back-end/src/store/reservation.ts`、`back-end/src/routes/orders.ts`。

---

## 3. 委托面板：分 tab 还是合并表

**场景**：前端展示用户的委托列表（未成交、已成交、已撤）。

**AI 初版**：AI 默认切成三个 tab。自测时下了一笔同价限价单瞬间全成交，跳进"未成交"tab 是空的——**我自己都误以为下单失败**。

**我发现的问题**：分 tab 把同一笔单的不同状态拆到不同视图，下完单要么在空 tab 看不到反馈，要么自动切 tab 打断用户当前焦点，体验都不好。

**我的 Prompt 节选**：
> 分 tab 不对。砍掉 tab，改成一张按时间倒序的合并表，状态用彩色标签区分；撤单按钮只在还能撤的状态下出现。

**修正后**：
- 一张表按时间倒序展示所有委托，状态彩签直接看
- 撤单按钮按状态条件渲染，已成交/已撤的行显示 `—`
- 部分成交的单同时显示"30/100 进度"和"撤单按钮"——分 tab 这两个信息必然分裂，合并表才能在同一行同时显示

代码：`front-end/src/components/OrdersPanel.vue`。

---

## 4. 前端响应式 Map 的坑

**场景**：行情数据用 `Map<股票代码, 股票>` 维护多支股票，WS 推送来了要更新界面。

**AI 初版**：AI 写成 `ref(new Map())`，更新走 `map.value.set(...)`。代码看起来合理，**但 WS 推送过来后界面不刷新**——Vue 的 ref 包 Map 时，调用 `set` 不会触发界面重渲染。

**我发现的问题**：Vue 的响应式系统对 Map 的内部操作（`.set` / `.delete`）在 ref 包装下不敏感，必须替换整个引用才能触发更新。

**我的 Prompt 节选**：
> `ref(Map)` 下 `.set()` 不响应式。两个候选：(a) 改 reactive Map；(b) 每次更新整体替换 ref 引用：`map.value = new Map(老的).set(k, v)`。选 (b)，因为 reactive Map 在不同 Vue 版本下行为不稳定。

**修正后**：
- 行情、委托、持仓三个 store 统一走"整体替换"模式：每次更新克隆一份新 Map 再赋值
- 3 支股票量级整体 clone 没性能负担；写法直白，看代码就知道触发刷新的是"赋值"而不是 `.set()`

代码：`front-end/src/stores/market.ts`。

---

## 5. 同价队列改链表：何时破例自己写

**场景**：复盘撮合代码时发现，同价队列用数组实现的话，每删除一个挂单都要把后面所有元素往前挪一格——一笔大单吃光一档累计代价随挂单数平方放大；撤单同理。需要"任意位置 O(1) 删除"。

**AI 初版**：让 sub agent 改成双向链表后，AI 默默手写了一个链表工具——**违反我"不自己造轮子"的原则**。

**我发现的问题**：跳表那种复杂结构装库我能理解，链表也手写就说不过去了。

**我的 Prompt 节选**：
> 跳表能找到 `proper-skip-list`，链表为什么找不到？是真没有还是你偷懒？真没有的话，破例的理由要写进文档，不能默默手写。

**修正后**：
- 跳表必装库（数百行、细节多，自己写容易出 bug）
- 但 JS 生态里**确实没有"侵入式链表"的库**——市面上的链表库都是"包一层节点对象"的方式，要做 O(1) 撤单还得额外维护一个"订单 ID → 节点"的反向查找表，反而比 30 行手写更绕
- 手写 `back-end/src/store/dlist.ts`，把前后指针挂在订单对象上；用 JS Symbol 作 key，让指针不会被 JSON 序列化泄漏到 API 响应里
- 撮合内循环改成走链表，maker 出簿 O(1)；撤单从"找档 O(log n) + 数组里找位置 O(m)"降到纯 O(log n)
- 原则修正：装库不是绝对的——复杂结构装库；30 行能写完且没合适库的可以自己写，但要在文档里说清楚为什么破例

代码：`back-end/src/store/dlist.ts`、`back-end/src/store/orders.ts`、`back-end/src/engine/matcher.ts`。

---

## 我的 prompt 习惯

1. **先看 AI 给什么，再判断是否打回**——不预先设候选，让 AI 暴露它默认取向
2. **打回时明确指出问题和修正方向**——"这版不对，原因是 X，改成 Y"，不让 AI 抛一堆方案让我挑
3. **不让 AI 自己造轮子**——能用第三方就用（跳表、JWT、WebSocket 库都不自己写）；破例时文档里写清理由
4. **对边界情况单独追问**——舍入、溢出、空状态、断线重连、自成交

最重要的：**写出的每一行代码，我都能讲清楚为什么这么写、AI 第一版错在哪、我是怎么发现并掰回来的**。
