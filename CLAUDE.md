# 项目级 Claude 指令

本项目的代码改动需要通过 Docker 重新部署才能在本地环境生效（前端构建产物、后端打包等都打在镜像里）。

## 每轮代码修改完成后必须重启容器

**触发条件**：本轮对话中只要修改过任何源代码（前端 / 后端 / Dockerfile / nginx.conf / docker-compose.yml 等），在向用户报告"完成"之前，必须运行：

```bash
bash scripts/redeploy.sh
```

该脚本位于仓库根目录的 `scripts/redeploy.sh`，会执行 `docker-compose down` + `docker-compose up -d --build` + `docker-compose ps`，确保改动落到运行中的容器里。

**执行规则**：

1. 即使本轮只改了一个文件、即使改动看起来很小，也要重启——避免"改完没生效"的低级问题。
2. 只是阅读 / 答疑 / 不落盘的探索（没有任何 Edit / Write）时不需要重启。
3. 重启脚本输出（容器状态）要简要回报给用户，确认服务是否健康。
4. 如果重启失败（构建错误 / 端口占用等），不要静默忽略——把错误信息贴出来并停下，等用户决定。
5. 报告"任务完成"之前必须先跑完重启；不要让用户自己去跑。
